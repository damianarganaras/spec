---
description: Start a new change using the experimental artifact workflow
---

Invoke the `ancleto-new` skill with the Skill tool and follow it exactly; do not improvise steps.

**Input**: The argument after `/cleto-new` is the change name (kebab-case), OR a description of what the user wants to build.

Work Item grounding (when the request references a Work Item) is owned by `@context-resolver`.
