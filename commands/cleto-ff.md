---
description: Create a change and generate all artifacts needed for implementation in one go
---

Invoke the `ancleto-ff` skill with the Skill tool and follow it exactly; do not improvise steps.

**Input**: The argument after `/cleto-ff` is the change name (kebab-case), OR a description of what the user wants to build.

Work Item grounding (when the request references a Work Item) is owned by `@context-resolver`.
