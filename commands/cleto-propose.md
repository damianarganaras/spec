---
description: Propose a new change - create it and generate all artifacts in one step
---

Invoke the `ancleto-propose` skill with the Skill tool and follow it exactly; do not improvise steps.

**Input**: The argument after `/cleto-propose` is the change name (kebab-case), OR a description of what the user wants to build.

Work Item grounding (when the request references a Work Item) is owned by `@context-resolver`.
