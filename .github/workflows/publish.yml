name: Publish

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Resolve tag
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            TAG="${GITHUB_REF_NAME}"
          else
            TAG="v${VERSION}"
          fi
          echo "PKG_VERSION=${VERSION}" >> "$GITHUB_ENV"
          echo "TAG=${TAG}" >> "$GITHUB_ENV"
          echo "tag a publicar: ${TAG} (version ${VERSION})"

      - run: npm ci

      - run: node --test test/*.test.js

      - name: Publish to npm (skipped if the version already exists)
        run: |
          if npm view "@ancleto/spec@${PKG_VERSION}" version >/dev/null 2>&1; then
            echo "ya publicado: ${PKG_VERSION} — se omite npm publish"
          else
            npm publish --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release (skipped if it already exists)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "el Release ${TAG} ya existe — se omite"
            exit 0
          fi
          # checkout puede dejar el tag como ref lightweight: traemos el objeto tag real
          git fetch --force origin "refs/tags/${TAG}:refs/tags/${TAG}" >/dev/null 2>&1 || true
          TYPE=$(git cat-file -t "refs/tags/${TAG}" 2>/dev/null || echo none)
          NOTES=""
          if [ "$TYPE" = "tag" ]; then
            NOTES=$(git for-each-ref "refs/tags/${TAG}" --format='%(contents)')
          fi
          if [ -z "$NOTES" ]; then
            echo "sin mensaje de tag anotado: se usan notas generadas"
            gh release create "$TAG" --title "$TAG" --generate-notes
          else
            gh release create "$TAG" --title "$TAG" --notes "$NOTES"
          fi

      # Canary corto y NO bloqueante: va despues del Release para no demorarlo.
      # npm puede tardar bastante en servir el tarball; si todavia no esta, avisa y sigue.
      - name: Canary - npm tarball disponible (no bloqueante)
        continue-on-error: true
        run: |
          URL="https://registry.npmjs.org/@ancleto/spec/-/spec-${PKG_VERSION}.tgz"
          for attempt in 1 2 3; do
            CODE=$(curl -s -o /dev/null -w "%{http_code}" -L "$URL")
            if [ "$CODE" = "200" ]; then
              echo "tarball OK: $URL"
              exit 0
            fi
            echo "intento ${attempt}/3: HTTP ${CODE}"
            if [ "$attempt" -lt 3 ]; then sleep 10; fi
          done
          echo "::warning::npm todavia no sirve el tarball de ${PKG_VERSION} (propagacion lenta, es normal). No hace falta hacer nada: la version y el README se actualizan solos. Para chequear mas tarde: npm view @ancleto/spec@${PKG_VERSION} dist.tarball"
