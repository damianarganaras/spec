---
description: Retrieve episodic project memory (.ancleto/memory.db) about a topic
---

Invoke the `ancleto-recall` skill with the Skill tool and follow it exactly; do not improvise steps.

**Input**: the argument after `/cleto-recall` is the topic to look up (e.g. an architectural decision, a subsystem, a bug).

Work Item grounding (when the request references a Work Item) is owned by `@context-resolver`.
